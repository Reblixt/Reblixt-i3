windowrule = tag +zen-browser-based-browser, class:([cC]hrom(e|ium)|[bB]rave-browser|Microsoft-edge|Vivaldi-stable)
windowrule = tag +firefox-based-browser, class:(Firefox|zen|librewolf)

# Force zen-browser-based browsers into a tile to deal with --app bug
windowrule = tile, tag:zen-browser-based-browser

# Only a subtle opacity change, but not for video sites
windowrule = opacity 1 0.97, tag:zen-browser-based-browser
windowrule = opacity 1 0.97, tag:firefox-based-browser

# Some video sites should never have opacity applied to them
windowrule = opacity 1.0 1.0, initialTitle:(youtube\.com_/|app\.zoom\.us_/wc/home)
